[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage settings are passed via command-line arguments.
# Example: pytest --cov=. --cov-report=html --cov-report=term-missing
addopts =
